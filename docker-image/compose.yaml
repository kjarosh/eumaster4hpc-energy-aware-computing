services:
  eumaster4hpc:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    privileged: true

  eumaster4hpc-dev:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - "./jupyter:/jupyter"
    profiles:
      - dev
    privileged: true
